git move <filename>
git add <filename>


git commit
git commit -a --amend

git pull --rebase
git rebase --configure

git push
git push origin MASTER:head

git checkout -b <new branch> [origin/master]

git clone https://github.com/sooryav/hippo
